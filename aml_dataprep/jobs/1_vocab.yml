$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
# ---------------------------------------------------------------------------
# Job 1/3: Build vocab (text -> id maps) from the raw TSV shards on cosmos.
# Reads:  ADLS Gen1 datastore bingads_algo_pipelines_c08
#         local/User/wenhlu/LRM_benchmark_v4/{train,val}
# Writes: workspace blobstore  derived/lrm_v4/vocab_v3
# CPU job (I/O bound). Deterministic: step1 sorts files + assigns ids sequentially.
# ---------------------------------------------------------------------------
display_name: lrm-dataprep-1-vocab
experiment_name: lrm-dataprep

# Singularity VC compute (raw ARM id; no azureml:// prefix)
compute: /subscriptions/72a1fe05-772c-4836-869f-761a5805fcd4/resourceGroups/Ads-Singularity-RG-01/providers/Microsoft.MachineLearningServices/virtualClusters/ads-shared-nd40
resources:
  instance_type: Singularity.ND96rs_v4
  instance_count: 1
queue_settings:
  job_tier: Standard

environment:
  image: mcr.microsoft.com/azureml/openmpi4.1.0-cuda11.8-cudnn8-ubuntu22.04:latest

code: ..            # repo root, so data_prep/ is available

inputs:
  train:
    type: uri_folder
    mode: ro_mount
    path: azureml://subscriptions/72a0fe10-0a76-4898-9b7b-640e6e236fdc/resourcegroups/wb-aml/workspaces/pconv-aml-offline/datastores/bingads_algo_pipelines_c08/paths/local/User/wenhlu/LRM_benchmark_v4/train
  val:
    type: uri_folder
    mode: ro_mount
    path: azureml://subscriptions/72a0fe10-0a76-4898-9b7b-640e6e236fdc/resourcegroups/wb-aml/workspaces/pconv-aml-offline/datastores/bingads_algo_pipelines_c08/paths/local/User/wenhlu/LRM_benchmark_v4/val

outputs:
  vocab:
    type: uri_folder
    mode: rw_mount
    path: azureml://datastores/workspaceblobstore/paths/derived/lrm_v4/vocab_v3

command: >-
  set -e &&
  echo "train shards: $(ls ${{inputs.train}}/train_chunk_*.tsv | wc -l)" &&
  echo "val shards:   $(ls ${{inputs.val}}/val_chunk_*.tsv | wc -l)" &&
  python data_prep/step1_collect_vocab_v2.py
  --train_dir ${{inputs.train}}
  --val_dir ${{inputs.val}}
  --output_dir ${{outputs.vocab}} &&
  echo "=== vocab_meta.json ===" && cat ${{outputs.vocab}}/vocab_meta.json
